import styles from './Greet.module.css'

const Greet = () => {
    return <h1 className={styles.heading} style={{backgroundColor:'cyan'}}>Hey there! Welcome to React Module!</h1>
}

export default Greet